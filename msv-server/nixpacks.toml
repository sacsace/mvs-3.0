[phases.setup]
# npm은 nodejs에 포함됨 — 별도 nixPkgs 'npm'은 Nixpacks 1.41+에서 빌드 실패
nixPkgs = ['nodejs']

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'npm run start:railway'
